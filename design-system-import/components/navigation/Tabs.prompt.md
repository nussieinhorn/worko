Underline tab bar for switching views within a project (Board, List, Timeline, Calendar).

```jsx
<Tabs value={view} onChange={setView} items={[
  {id:"board", label:"Board"},
  {id:"list", label:"List"},
]} />
```
