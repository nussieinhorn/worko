Circular user avatar with initials fallback; `AvatarGroup` overlaps several with a +N chip for team members.

```jsx
<Avatar name="Maya Chen" size="md" />
<AvatarGroup users={[{name:"Maya Chen"},{name:"Sam Ito"},{name:"Lee Roy"}]} max={3} />
```
