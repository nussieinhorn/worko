The base surface for WORKO content — task cards, project cards, panels. White, soft shadow, rounded 16px.

```jsx
<Card interactive onClick={open}>
  <h3>Q3 Launch</h3>
  <p>12 open tasks</p>
</Card>
```

`interactive` lifts the card and deepens the shadow on hover. Override `padding` as needed.
