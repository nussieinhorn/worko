Checkbox for forms and task lists. Use `round` for WORKO task items — checking strikes the label through.

```jsx
<Checkbox round checked={done} onChange={setDone} label="Draft the launch email" />
```
