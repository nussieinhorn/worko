A square, icon-only button for toolbars and card actions — pair with a Lucide icon and always pass an accessible `label`.

```jsx
<IconButton label="More options" variant="ghost"><MoreIcon /></IconButton>
```

Variants `primary | secondary | ghost`; sizes `sm | md | lg`. Press scales to 0.94.
