# Supabase client connection. These are public-by-design browser credentials —
# they ship inside the JS bundle of every deploy — so committing them is safe.
# Access control lives in Supabase RLS policies, not in these values.
VITE_SUPABASE_URL=https://avnavgtsxoqizwvyhgrd.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_H1eN7gIhDIbE-YuwYAifRQ_rqAIiG_o
